# Core AI & voice
google-generativeai
python-dotenv
speechrecognition
pyttsx3
pyaudio

# Automation & utilities
pyautogui
yt-dlp
requests

# Email handling
smtplib   # built-in, no need to install separately
email     # built-in, no need to install separately

# Optional (for cloud/local deployment enhancements)
flask      # if deploying as web app
streamlit  # if using Streamlit interface
